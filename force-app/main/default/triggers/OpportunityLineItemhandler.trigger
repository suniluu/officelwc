trigger OpportunityLineItemhandler on OpportunityLineItem (after insert,after delete,after update,before insert) 
{ 
    /*if(trigger.isafter && trigger.isinsert)
    {
        OpportunityLineItemHandler.creatingAssest(trigger.new);
    }
     if(trigger.isafter && trigger.isinsert)
    {
        OpportunityLineItemHandler.creatingquotationWhenOpportunityLineItemIsInserted(trigger.new);
    }
    if(trigger.isafter && trigger.isdelete)
    {
        OpportunityLineItemHandler.deletingOpportunityWhenopportunityLineitemIsDeleted(trigger.old);
    }
if(trigger.isafter && trigger.isupdate)
    {
        OpportunityLineItemHandler.total(trigger.new);
    }
    
    if(trigger.isbefore && trigger.isinsert)
    {
        OpportunityLineItemHandler.productfamily(trigger.new);
    }*/
}