trigger EmailAlertForCandAndPos on Job_Application__c (after insert,after update) 
{
 /*   if(trigger.isafter && trigger.isupdate)
    {
        EmailAlertForCandAndPos.EmailsAlert(trigger.new);
    }
*/
}