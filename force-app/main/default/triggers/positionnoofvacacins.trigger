trigger positionnoofvacacins on Job_Application__c (after insert,after update) {
/*    if(trigger.isafter&& (trigger.isinsert || trigger.isupdate ))
    {
        position.job(trigger.new);
    }
  if(trigger.isafter&& trigger.isinsert )
    {
        position.job(trigger.new);
    }*/

}